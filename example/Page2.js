import React from 'react';
import PropTypes from 'prop-types';

const Page2 = props => {
    return (
        <div>
            hello Page2
        </div>
    );
};

Page2.propTypes = {
    
};

export default Page2;