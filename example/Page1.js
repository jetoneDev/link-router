import React from 'react';
import PropTypes from 'prop-types';

const Page1 = props => {
    console.log(1);
    return (
        <div>
            hello Page1
        </div>
    );
};

Page1.propTypes = {
    
};

export default Page1;